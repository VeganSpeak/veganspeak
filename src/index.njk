---
layout: default
---

<div class="max-w-screen-md mx-auto spacer">
	<p class="p-8 prose bg-neutral-100 lg:prose-xl">
		VSpeak is a collection of the most heard arguments against veganism. This is an open source work in progress and we welcome your <a href="https://github.com/VeganSpeak/veganspeak/" target="_blank" rel="noopener noreferrer">contributions or improvements</a>.
	</p>
	{# <h3 class="font-bold leading-none tracking-tight fl-text-2xl">Common Arguments</h3>

	<ul class="space-y-2 fl-text-lg spacer-t-sm spacer-b">
		{% for post in collections.popular | sort(false, false, 'data.trendOrder') %}
			<li class="pb-2 border-b border-dotted border-primary-100">
				<a class="block" href="{{ post.url | url }}">
					{{ post.data.title }}
				</a>
			</li>
		{% endfor %}
	</ul>

	<h3 class="font-bold leading-none tracking-tight fl-text-2xl">All Arguments</h3> #}

	<ol class="space-y-2 fl-text-lg spacer">
		{# {% for post in collections.arguments | sort(false, false, 'data.order') %} #}
		{% for post in collections.arguments %}
			<li class="pb-2 border-b border-dotted border-primary-100">
				<a class="block" href="{{ post.url | url }}">
					{% if post.data.trendOrder %}
						<span class="pr-2 lg:-ml-9">★</span>
					{% endif %}
					{{ post.data.title }}
				</a>
			</li>
		{% endfor %}
	</ol>
</div>